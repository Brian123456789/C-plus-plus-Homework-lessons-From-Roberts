#include <iostream>

using namespace std;

int main()
{
    int a = 98;
    int b = 76;

    if(a > b){ // Tests if a is greater than b.
    // if (test) test parameter/condition {run code}

        cout << "Bucky is AWESOME!!!!" << endl;
    }//body uses curly brackets, code runs between them. They are "containers"

    return 0;
}
